import React from 'react'
import CompanyFrame from '../components/CompanyFrame'

export const CompanyContainer = ({dispatch, router}) => {
  return <CompanyFrame dispatch={dispatch} router={router} />
}
