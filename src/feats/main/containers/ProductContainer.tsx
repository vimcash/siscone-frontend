import React from 'react'
import ProductFrame from '../components/ProductFrame'

export const ProductContainer = ({ dispatch, router }) => {
  return <ProductFrame dispatch={dispatch} router={router} />
}
