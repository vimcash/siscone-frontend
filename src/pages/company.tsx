import { CompanyContainer } from '@/feats/main/containers/CompanyContainer'
import React from 'react'

const company = ({ dispatch, router }) => {
  return <CompanyContainer dispatch={dispatch} router={router} />
}

export default company