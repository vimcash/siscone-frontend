/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "standalone",
  reactStrictMode: true,
  // env: {
  //   NEXT_PUBLIC_PRUEBA: "PRUEBA 1",
  // },
};

export default nextConfig;